{
  "name": "Retail Digital Twin",
  "install": "./.cursor/scripts/cloud-agent-install.sh",
  "terminals": [
    {
      "name": "streamlit",
      "command": "export PATH=\"${HOME}/.local/bin:${PATH}\" && streamlit run app.py --server.headless true --server.port 8501 --server.address 0.0.0.0"
    }
  ],
  "ports": [8501]
}
